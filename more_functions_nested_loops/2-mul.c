#include "main.h"

/**
 * mul - Print the result of a multiplication between two ints
 *
 * @a: first number
 * @b: second number
 *
 * Return: Always 1 or 0
 */
int mul(int a, int b)
{
	return (a * b);
}
