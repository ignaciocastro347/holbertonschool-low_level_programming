#include "main.h"

/**
 *  * _isdigit - Print 1 if c is a digit
 *   *
 *    * @c: A character
 *     *
 *      * Return: Always 1 or 0
 *       */
int _isdigit(int c)
{
		if (c >= '0' && c <= '9')
					return (1);
			return (0);
}
